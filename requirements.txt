flask==3.1.3
gunicorn==22.0.0
# Ranges, not exact pins: Render's build platform doesn't always have a
# prebuilt wheel for one specific patch version, which makes pip fall back
# to a from-source build - and scipy/numpy/scikit-learn source builds need a
# Fortran/C compiler and BLAS/LAPACK that a minimal build image doesn't have.
# A range lets pip pick whichever version in it actually has a wheel.
numpy>=1.26.4,<2.0
scikit-learn>=1.5.1,<1.6
scipy>=1.13,<1.15
certifi>=2024.2.2
joblib==1.5.1
pymongo[srv]==4.11.2
python-dotenv==1.2.2
python-json-logger==2.0.7
waitress==3.0.1
cachetools==5.5.0
flask-limiter==3.8.0
flask-cors==6.0.0
sentry-sdk[flask]==2.19.2
